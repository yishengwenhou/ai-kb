spring:
  application:
    name: backend
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/ai_kb?characterEncoding=UTF-8&useUnicode=true&useSSL=false&tinyInt1isBit=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 20030315
  sql:
    init:
      mode: never

# JWT配置
jwt:
  secret: thisIsASecretKeyForJwtTokenThatShouldBeLongEnoughAndSecure
  access:
    expiration: 3600
  refresh:
    expiration: 604800

# MyBatis-Plus配置
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      not-delete: 0